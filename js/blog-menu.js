(function ($) {
  Drupal.behaviors.da_vinciTheme = {
    attach: function (context) {
/*      $('#block-blog > ul > li > a').click(function( event ) {
        if ($(this).parent().find('.menu').is(':visible')) {
        }
        else {
          event.preventDefault();
          $(this).parent().find('.menu').show();
        }
      });*/

    }
  }
})(jQuery);